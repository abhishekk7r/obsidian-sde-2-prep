## Convention
Same as `HLD/` — one file per question. Add a file here per LLD problem as they come up (e.g. object modeling, class diagrams, design-pattern-heavy questions like Parking Lot, Elevator System, Rate Limiter).
